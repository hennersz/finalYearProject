---
- hosts: bootstrapNodes

  tasks:
    - name: run bootstrapping server
      shell: ~/p2ppki/ENV/bin/activate && twistd -noy ~/p2ppki/bin/server.tac

- hosts: VMs

  tasks: 
    - name: Run the program
      shell: . ~/p2ppki/ENV/bin/activate && python ~/p2ppki/p2ppki/core.py
